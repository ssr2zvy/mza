[workspace]

[package]
name = "lexicon-release-build"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
tar = "0.4"
toml = "0.8"
xz2 = "0.1"

[dev-dependencies]
tempfile = "3"
